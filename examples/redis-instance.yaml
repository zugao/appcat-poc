apiVersion: appcat.vshn.io/v1alpha1
kind: XVSHNRedis
metadata:
  name: my-redis
  namespace: default
spec:
  writeConnectionSecretToRef:
    name: redis-credentials
    namespace: default
  size:
    cpu: "1000m"
    memory: "4Gi"
    disk: "16Gi"
  replicas: 1